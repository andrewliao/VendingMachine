package pack1;

public class Apreview {
	public static void main(String[] args) 
		throws java.io.IOException {
		/*
		char ch, ignore, answer = 'K';
		
		do {
			System.out.println("I'm thinking of a letter between A and Z.");
			System.out.print("Can you guess it: ");
			
			//read a character
			ch = (char) System.in.read();
			
			//discard any other characters in the input buffer
			do {
				ignore = (char) System.in.read();
			} while (ignore != '\n');
			
			if (ch == answer) {
				System.out.println("You are right!");
			} else {
				System.out.println("Sorry you're too...");
				if (ch > answer) {
					System.out.println("too high!");
				} else {
					System.out.println("too low!");
				}
			}
		
		} while (answer != ch);
		
		//break labels also look at continue with labels
		
		int x = 0; int y = 0;
		
		stop1: for(x = 0; x < 5; x++) {
					for (y = 0; y < 5; y++) {
						if(y == 2) {
							break stop1;
						}
							System.out.println("x and y: " + x + " " + y);
					}
				} 
				
			System.out.println();
			
			for(x = 0; x < 5; x++) {
		stop2: 		{
						for (y = 0; y < 5; y++) {
							if (y == 2) {
								break stop2;
							}
							System.out.println("x and y: " + x + " " + y);
						}
					}
			}
		
		//space counter
		
		char ch;
		int spaces = 0;
		
		do {
			ch = (char) System.in.read();
			if (ch == ' ') {
				spaces++;
			}
		} while(ch != '.');
		
		System.out.println("Spaces: " + spaces);
		
		
		
		
		//charcter case changer 
		char ch; 
		int changes = 0;
		System.out.println("Type your string of letters you want to change the case of and type a period at the end!");
		do {
			ch = (char) System.in.read();
			if (ch >= 'a' && ch <= 'z') {
				System.out.print((char) (ch - 32) + " ");
				changes++;
			}
			if (ch >= 'A' && ch <= 'Z') {
				System.out.print((char) (ch + 32) + " ");
				changes++;
			}
		} while (ch != '.');
		
		System.out.println();
		System.out.println("Your total number of case changes is " + changes + "!");
		
		*/
		
		//try to call another classes main
		
		
		
	
	}
}
